---
sidebar: sidebar
permalink: task-assign-svm-admin-access-sm.html
keywords: netapp, system manager, ontap, administration, svm, storage vm, volume, vserver, dashboard, administrator, tenant, multi-tenancy, protocol, network interface, performance, assign
summary: As an ONTAP cluster administrator, you can assign SVM administrator access to the person responsible for managing and monitoring the SVM.
---
= Assign SVM administrator access with ONTAP System Manager
:toclevels: 1
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
Beginning with ONTAP 9.19.1, ONTAP cluster administrators can assign SVM administrator dashboard access to the person who manages the SVM.

This is particularly useful in a tenanted ONTAP environment, where the cluster administrator can provide an SVM dashboard for each tenant.

Cluster administrators can allow the SVM administrator the ability to create volumes.

== Step 1: Prepare the management network interface for the SVM dashboard 

Before you assign the SVM dashboard to the administrator, check to see if the SVM has a management network interface. If you do not already have a network interface, you can create one. 

.Steps 

. In System Manager, select *Network interfaces > Overview* and then select *Add* to create a network interface. 

. Select *Storage VM management* as the network interface type. 

. Specify the *IP address* and the *Subnet mask* for the SVM management network interface. 

== Step 2: Verify the vsadmin user account configuration 

When an SVM is created, ONTAP automatically creates the vsadmin user account. Before you assign the SVM dashboard to the administrator, ensure that the vsadmin user account has a password and is unlocked. 

.Steps 

. Open the System Manager dashboard as the cluster administrator. 

. Select *Cluster > Storage VMs*.  

. Select the SVM, select *Settings* and then select *Security > Users and roles*.  

. Next to the *vsadmin* user, select image:icon_kabob.gif[Menu options icon] and, if needed, select: 

.. *Change password* to update the vsadmin password. 

.. *Unlock* to make sure it is usable. 

== Step 3: Allow SVM administrators to add volumes (optional) 

If you want SVM administrators to add local volumes, you can grant permission.

.Steps

. Open the System Manager dashboard and sign in as the cluster administrator. 

. Under *Cluster*, select *Storage VMs*. 

. Select image:icon_kabob.gif[Menu options icon] for the applicable SVM. 

. Select *Edit*.  

. Under *Resource Allocation*, select the *Limit volume creation to preferred local tiers* checkbox.  

. By default, all local tiers are included when you select the checkbox. You can remove specific tiers if necessary. If you remove all local tiers or leave the checkbox blank, SVM administrators cannot provision local volumes. 

. Select *Save*. 

== Step 4: Assign SVM administrator access 

To assign SVM dashboard access, provide the SVM management IP address and the vsadmin password to each SVM administrator. 

.Steps 

. In System Manager, select *Network interfaces > Overview* and then select the *Management* network interface. 

. Select the IP address and share it, along with the *vsadmin* username and password with the SVM administrator. 


.Related topics:

* link:disks-aggregates/assign-aggregates-svms-task.html[Assign ONTAP local tiers to storage VMs (SVMs)]      
* link:authentication/create-svm-user-accounts-task.html#cluster-and-svm-administrators[Learn about creating ONTAP login accounts]
* link:concepts/cluster-svm-administrators-concept.html[Cluster and SVM administration]
* link:https://docs.netapp.com/us-en/ontap-cli/security-login-password.html#description[security login password]

// 2026 February 4-18, ONTAPDOC-3605 and NADIAA
