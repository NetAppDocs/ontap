---
sidebar: sidebar
permalink: anti-ransomware/recover-data-task.html
keywords: Restore data after an attack, anti-ransomware backup, restore data from snapshots, potential ransomware attack, ransomware attack, recovery snapshots are deleted, recover deleted snapshots, autonomous, ransomware, protection, arp
summary: "ARP creates snapshots to protect against a potential ransomware threat. You can restore data from these ARP snapshots or other snapshots."
---

= Restore data from ONTAP ARP snapshots after a ransomware attack
:icons: font
:imagesdir: ../media/

[.lead]
Autonomous Ransomware Protection (ARP) creates snapshots to protect against a potential ransomware threat. You can use one of these ARP snapshots or another snapshot of your volume to restore data.

.About this task
The ARP creates snapshots with one of the following prepended names:

* `Anti_ransomware_periodic_backup`: Used in ONTAP 9.17.1 and later for snapshots created at regular intervals. For example, `Anti_ransomware_periodic_backup.2025-06-01_1248`.
* `Anti_ransomware_attack_backup`: Used in ONTAP 9.17.1 and later for snapshots created in response to abnormalities. For example, `Anti_ransomware_attack_backup.2025-08-25_1248`.
* `Anti_ransomware_backup`: Used in ONTAP 9.16.1 and earlier with snapshots that are created in response to abnormalities. For example, `Anti_ransomware_backup.2022-12-20_1248`.

To restore from a snapshot other than the `Anti_ransomware` snapshot after a system attack is identified, you must first release the ARP snapshot.

If no system attack is reported, you must first restore from the `Anti_ransomware` snapshot then complete a subsequent restoration of the volume from the snapshot you choose.

.More information about SnapMirror relationships and ARP snapshots
[%collapsible]
====
If the ARP-protected volume is part of a SnapMirror relationship, you'll need to manually update all mirror copies of the volume after restoring it from a snapshot. If you skip this step, the mirror copies might become unusable and need to be deleted and recreated.

If the ARP-protected volume is part of a SnapMirror synchronous or SnapMirror active sync relationship in ONTAP 9.19.1 RC, additional recovery considerations apply:

* ARP snapshots are created and retained only on the primary volume. They are not replicated to the secondary volume as part of SnapMirror synchronous or SnapMirror active sync.
* The ARP enablement state is not replicated during failover. You need to link:enable-task.html[enable ARP again] on the recovery volume after failover.
* Volume-level restore (`volume snapshot restore`) on SnapMirror synchronous or SnapMirror active sync volumes might require that you temporarily quiesce or break the SnapMirror synchronous or SnapMirror active sync relationship.
* In many cases, you can avoid breaking SnapMirror synchronous or SnapMirror active sync by using FlexClone or file-level restore operations from ARP or other snapshots on the primary volume.
====

.Before you begin
If you are responding to an abnormal activity warning, link:respond-abnormal-task.html[you must first categorize it as a potential ransomware attack] before proceeding with data restoration.

.Steps

Use this page when you need to restore data after ARP detects an abnormality. If you need to classify abnormal activity and clear ARP notices, use link:respond-abnormal-task.html[Respond to abnormal activity detected by ONTAP ARP].

== Shared recovery constraints

* For ONTAP 9.15.1 and earlier, releasing the ARP snapshot lock deletes ARP snapshots immediately. Release the lock only if you do not plan to restore from an ARP snapshot.
* In CLI workflows, releasing the anti-ransomware lock before restoring from earlier snapshots is only required when you use `volume snapshot restore`. It is not required for FlexClone, single-file SnapRestore, or similar methods.
* If the volume is part of a SnapMirror synchronous or SnapMirror active sync SAN relationship in ONTAP 9.19.1 RC and you plan a volume-level restore, quiesce or break the relationship before restore and then reestablish protection after restore.

[[restore-after-system-attack]]
== Restore after a system attack

Choose one of these flows based on the snapshot source:

* <<restore-from-arp-snapshot,Flow 1: Restore from an ARP snapshot>>
* <<restore-from-non-arp-snapshot,Flow 2: Restore from an earlier non-ARP snapshot>>

[[restore-from-arp-snapshot]]
=== Flow 1: Restore from an ARP snapshot

[role="tabbed-block"]
====
.System Manager
--
. Select *Storage > Volumes*, then select the volume and *Snapshot Copies*.

. Select image:icon_kabob.gif[Menu options icon] next to the ARP snapshot you want to restore, then select *Restore*.

. After the restore completes, link:respond-abnormal-task.html#arp-snapshot-decision-sm[record your decision and clear notices].
--

.CLI
--
. Restore the contents of a volume from the ARP snapshot:

include::../_include/restore-snapshots-cli.adoc[]

. After the restore completes, link:respond-abnormal-task.html#arp-snapshot-decision-cli[record your decision and clear notices].
--
====

[[restore-from-non-arp-snapshot]]
=== Flow 2: Restore from an earlier non-ARP snapshot

[role="tabbed-block"]
====
.System Manager
--
. Release the lock on the ARP snapshot:
+
.. Select *Storage > Volumes*.
.. Select *Security* then *View Suspected File Types*.
.. Mark the files as *Potential ransomware attack*.
.. Select *Update* and *Clear Suspected File Types*.

. Select *Storage > Volumes*, then select the volume and *Snapshot Copies*.

. Select image:icon_kabob.gif[Menu options icon] next to the non-ARP snapshot you want to restore, then select *Restore*.
--

.CLI
--
. If you are restoring to an earlier snapshot using `volume snapshot restore`, mark the attack as potential ransomware (`-false-positive false`) and clear suspect files to release the lock:
+
[source,cli]
----
anti-ransomware volume attack clear-suspect -vserver <svm_name> -volume <vol_name> [<extension identifiers>] -false-positive false
----
+
Use one of the following parameters to identify the extensions:
+
* `[-seq-no _integer_]`: Sequence number of the file in the suspect list.
* `[-extension _text_, … ]`: File extensions
* `[-start-time _date_time_ -end-time _date_time_]`: Starting and ending times for the range of files to be cleared, in the form "MM/DD/YYYY HH:MM:SS".

. Restore the contents of the volume from the earlier snapshot:

include::../_include/restore-snapshots-cli.adoc[]
--
====

[[restore-without-attack]]
== Flow 3: Restore when a system attack is not identified

When no attack is identified, first restore from the ARP snapshot and then restore from the snapshot you choose.

[role="tabbed-block"]
====
.System Manager
--
. Select *Storage > Volumes*, then select the volume and *Snapshot Copies*.
. Select image:icon_kabob.gif[Menu options icon] then choose the `Anti_ransomware` snapshot.
. Select *Restore*.
. Return to the *Snapshot Copies* menu, then choose the snapshot you want to use.
. Select *Restore*.
--

.CLI
--
. Restore from the ARP snapshot first:

include::../_include/restore-snapshots-cli.adoc[]

. Repeat the restore and select the non-ARP snapshot you want to use.

Learn more about `volume snapshot` in the link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=volume+snapshot[ONTAP command reference^].
--
====

.Related information
* link:https://kb.netapp.com/Advice_and_Troubleshooting/Data_Storage_Software/ONTAP_OS/Ransomware_prevention_and_recovery_in_ONTAP[NetApp Knowledge Base: Ransomware prevention and recovery in ONTAP^]
* link:https://docs.netapp.com/us-en/ontap-cli/[ONTAP command reference^]

// 2026-3-10, ontapdoc-3689
// 2025 Aug 18, ONTAPDOC-1127
// 2025 Mar 11, ONTAPDOC-2758
// 2024-10-10, ontapdoc-2343
// 2024-02-26, #1264
// 2023 Mar 08, Jira 894
// 2022-08-25, BURT 1499112
// 2022-06-10, jira-kda-1532
// 2021-12-08, BURT 1444760
// 2021-10-29, Jira IE-353